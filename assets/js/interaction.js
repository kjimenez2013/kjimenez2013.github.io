const prevPostButton = document.getElementById("previous")
const nextPostButton = document.getElementById("next")

prevPostButton.addEventListener("click", () => {
  console.log("previous post")
})

nextPostButton.addEventListener("click", () => {
  console.log("next post")
})